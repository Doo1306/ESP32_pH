<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ESP32pH.Views.SettingView"
             xmlns:viewmodel="clr-namespace:ESP32pH.ViewModels"
             xmlns:convertor="clr-namespace:ESP32pH.Convertors"
             xmlns:controls="clr-namespace:ESP32pH.Controls"
             Title="Setting"
             >

    <ContentPage.BindingContext>
        <viewmodel:SettingViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Resources>
        <convertor:PermissionToVisibilityConverter x:Key="PermissionToVisibilityConverter"/>
    </ContentPage.Resources>

    <ScrollView>
        <StackLayout>
            <!-- Gradient background -->
            <StackLayout.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#6DD0F0" Offset="0.0" />
                    <GradientStop Color="#DDB1F0" Offset="1.0" />
                </LinearGradientBrush>
            </StackLayout.Background>

            <!-- Time Span -->
            <Border Style="{StaticResource WhiteCardStyle}" IsVisible="{Binding CurrentLoginModel.Permission,Converter={StaticResource PermissionToVisibilityConverter},ConverterParameter=Maker}">
                <HorizontalStackLayout Spacing="10" VerticalOptions="Center">
                    <Label Text="Time Interval" WidthRequest="180" VerticalOptions="Center" />
                    <Entry x:Name="TimeSpanEntry" Text="{Binding TimeInterval,Mode=TwoWay}" Keyboard="Numeric"/>
                    <Label Text="ms" VerticalOptions="Center"/>
                </HorizontalStackLayout>
            </Border>

            <!-- Door Delay -->
            <Border Style="{StaticResource WhiteCardStyle}" IsVisible="{Binding CurrentLoginModel.Permission,Converter={StaticResource PermissionToVisibilityConverter},ConverterParameter=Admin}">
                <HorizontalStackLayout Spacing="10" VerticalOptions="Center">
                    <Label Text="Time Delay request Door change" WidthRequest="250" VerticalOptions="Center" />
                    <Entry x:Name="DoorDelayEntry" Text="{Binding TimeDelay,Mode=TwoWay}" Keyboard="Numeric" />
                    <Label Text="ms" VerticalOptions="Center"/>
                </HorizontalStackLayout>
            </Border>

           
           
            <!-- pH Min/Max -->
            <Border Style="{StaticResource WhiteCardStyle}" IsVisible="{Binding CurrentLoginModel.Permission,Converter={StaticResource PermissionToVisibilityConverter},ConverterParameter=Admin}">
                <VerticalStackLayout Spacing="10">
                    <HorizontalStackLayout Spacing="10">
                        <Label Text="pH Min" WidthRequest="150" VerticalOptions="Center" />
                        <Entry x:Name="PhMinEntry" Text="{Binding pHMin}" Keyboard="Numeric"  />
                        <Label Text="pH" VerticalOptions="Center"/>
                    </HorizontalStackLayout>

                    <HorizontalStackLayout Spacing="10">
                        <Label Text="pH Max" WidthRequest="150" VerticalOptions="Center" />
                        <Entry x:Name="PhMaxEntry" Text="{Binding pHMax}" Keyboard="Numeric"  />
                        <Label Text="pH" VerticalOptions="Center"/>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Border>

            <!-- Test mode -->
            <Border Style="{StaticResource WhiteCardStyle}" IsVisible="{Binding CurrentLoginModel.Permission,Converter={StaticResource PermissionToVisibilityConverter},ConverterParameter=Maker}">
                <StackLayout Orientation="Horizontal">
                    <controls:CustomToggleSwitch IsToggled="{Binding TestMode}"
                       HeightCustom="50"
                       WidthCustom="120"
                       OnColor="Green"
                       OffColor="LightGray"
                       OnText="On Test"
                       OffText="Off Test"
                       FontSize="12"
                       Margin="0,0,10,0"/>
                    <Label Text="Test Mode Change" VerticalOptions="Center" />
                </StackLayout>
            </Border>

            <!-- Analog Offsets -->
            <Border Style="{StaticResource WhiteCardStyle}" IsVisible="{Binding CurrentLoginModel.Permission,Converter={StaticResource PermissionToVisibilityConverter},ConverterParameter=Maker}">
                <VerticalStackLayout Spacing="10">

                    <HorizontalStackLayout Spacing="10">
                        <Label Text="Analog offset X0" WidthRequest="150" VerticalOptions="Center" />
                        <Entry x:Name="OffsetX0Entry" Text="{Binding OffsetX0}" Keyboard="Numeric"  />
                        <Label Text="AnalogValue" VerticalOptions="Center"/>
                    </HorizontalStackLayout>

                    <HorizontalStackLayout Spacing="10">
                        <Label Text="Analog offset Y0" WidthRequest="150" VerticalOptions="Center" />
                        <Entry x:Name="OffsetY0Entry" Text="{Binding OffsetY0}" Keyboard="Numeric"  />
                        <Label Text="V" VerticalOptions="Center"/>
                    </HorizontalStackLayout>

                    <HorizontalStackLayout Spacing="10">
                        <Label Text="Analog offset X1" WidthRequest="150" VerticalOptions="Center" />
                        <Entry x:Name="OffsetX1Entry" Text="{Binding OffsetX1}"  Keyboard="Numeric"  />
                        <Label Text="AnalogValue" VerticalOptions="Center"/>
                    </HorizontalStackLayout>

                    <HorizontalStackLayout Spacing="10">
                        <Label Text="Analog offset Y1" WidthRequest="150" VerticalOptions="Center" />
                        <Entry x:Name="OffsetY1Entry" Text="{Binding OffsetY1}" Keyboard="Numeric" />
                        <Label Text="V" VerticalOptions="Center"/>
                    </HorizontalStackLayout>

                    <HorizontalStackLayout Spacing="10">
                        <Label Text="Analog offset X2" WidthRequest="150" VerticalOptions="Center" />
                        <Entry x:Name="OffsetX2Entry" Text="{Binding OffsetX2}" Keyboard="Numeric"  />
                        <Label Text="AnalogValue" VerticalOptions="Center"/>
                    </HorizontalStackLayout>

                    <HorizontalStackLayout Spacing="10">
                        <Label Text="Analog offset Y2" WidthRequest="150" VerticalOptions="Center" />
                        <Entry x:Name="OffsetY2Entry" Text="{Binding OffsetY1}" Keyboard="Numeric" />
                        <Label Text="V" VerticalOptions="Center"/>
                    </HorizontalStackLayout>

                </VerticalStackLayout>
            </Border>

            <!-- Buttons -->
            <Border Style="{StaticResource WhiteCardStyle}" IsVisible="{Binding CurrentLoginModel.Permission,Converter={StaticResource PermissionToVisibilityConverter},ConverterParameter=Admin}">
                <HorizontalStackLayout HorizontalOptions="Center" Spacing="30" Margin="0,20,0,0">
                    <Button Text="Accept"
                        Style="{StaticResource ButtonRemain}"
                        TextColor="White"
                        WidthRequest="120"
                        Command="{Binding  SaveSettingCommand}" />
                    <Button Text="Cancel"
                        Style="{StaticResource ButtonRemain}"                       
                        TextColor="White"
                        WidthRequest="120"
                        Command="{Binding CancelSettingCommand}" />
                </HorizontalStackLayout>
            </Border>

        </StackLayout>
    </ScrollView>

</ContentPage>